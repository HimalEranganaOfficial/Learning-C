#include <stdio.h>

int main() {
    int a, b, c;
    printf("Enter the three integers:\n");
    scanf("%3d %3d %3d", &a, &b, &c);
    printf("You entered: %d, %d, %d\n", a, b, c);
    return 0;
}